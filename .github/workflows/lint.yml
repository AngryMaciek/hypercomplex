name: cpplint

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  cpplint:
    runs-on: ubuntu-20.04

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Install cpplint
        run: pip install cpplint

      - name: Static Code Analysis
        run: |
          /home/runner/.local/bin/cpplint --quiet hypercomplex/hypercomplex.h
          /home/runner/.local/bin/cpplint --quiet hypercomplex/hypercomplex.cpp
